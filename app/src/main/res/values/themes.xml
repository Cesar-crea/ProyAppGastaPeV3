<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppGastaPe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AppGastaPe" parent="Base.Theme.AppGastaPe" />
    <style name="title">
        <item name="android:textSize">25sp</item>

        <item name="android:textColor">@color/rojo</item>

        <item name="android:textAlignment">center</item>

        <item name="android:textStyle">bold</item>
    </style>

    <!-- Estilo personalizado para el indicador activo del BottomNavigation -->
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <!-- Cambia el color del fondo del ítem seleccionado a negro -->
        <item name="android:color">@color/white</item>
    </style>
</resources>